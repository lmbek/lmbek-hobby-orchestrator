# Documentation — staging environment.
# Uses pre-built image from the container registry.
# Usage: docker compose -f docker-compose.docs.stage.yml up -d

services:
  docs:
    image: registry.example.com/lmbek-hobby-docs:stage
    restart: unless-stopped
    networks:
      - hobby-net

networks:
  hobby-net:
    driver: bridge
